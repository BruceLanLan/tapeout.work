# TapeOut Intelligence 生产 UI/UX 审计（第 1 轮）

- 审计时间：2026-08-24 GMT+8
- 生产地址：<https://tapeout-public-monitor.tapeout-labs.workers.dev/?v=qa-20260824>
- 观察范围：桌面首屏、协议脉冲、时间分析入口。

## 已确认的高影响问题

| 优先级 | 发现 | 影响 | 修复方向 |
| --- | --- | --- | --- |
| P0 | 左侧“已观察的 TapeOut 全协议活动”以**当前 UTC 日**汇总，右侧时间序列默认是**过去 7 天 / 按日 / 北京时间**。两个相邻面板的时间范围和时区不同，容易被误读为同一口径。 | 造成数据语义冲突；用户无法一眼判断左侧 KPI 与右图是否可比较。 | 让左侧脉冲卡随右侧范围 / 时区（必要时粒度）联动；明确“所选周期汇总”。 |
| P1 | Hero 主标题在中文下占据四行，视觉权重远高于实时数据与操作入口；导航、来源说明和 KPI 的相对层级偏弱。 | 首屏的产品身份强于数据价值；向下阅读成本高。 | 收紧中文标题字号 / 行高，缩短引导语并提高实时状态与核心指标的首屏密度。 |
| P1 | 协议活动右侧四个筛选器在桌面已较密集，Metric 被单独折行；左侧还保留一组不同口径的 current-UTC KPI。 | 控制复杂、认知负担高，尤其会放大口径冲突。 | 将“范围 / 粒度 / 时区”做成紧凑的 segmented control 或统一工具栏；指标选择独立为次级控件；摘要联动。 |
| P2 | 英中混排在同一层级出现，例如 `TAPEOUT PROTOCOL · 协议数据`、英文说明块和中文卡片标题。 | 页面主语言不够确定，阅读流存在切换成本。 | 中文模式下将机制说明完全中文化；保留必要的 Processor / Circuit / BNB 等技术名词。 |
| P2 | Header 导航与同步状态按钮的字重、尺寸较小，信息密度虽高但可点击性和扫描性偏弱。 | 日常回访用户不易快速识别当前数据新鲜度、入口与模式。 | 提升导航命中面积、active 状态和数据新鲜度的对比度。 |

## 已确认的数据正确性

- 当前摘要为 722 个公开 Processor、346 个发生过铸造的 Processor、12,433 条已观察 Circuit、55 个已全量铸造的 Processor。
- 默认时间图的 7 天按日北京时区序列显示真实 D1 覆盖 4.9 天，并明确首桶部分覆盖；该边界本身正确。
- 左侧 current-UTC 卡的 0 / 7 / 177 / 6 是不同时间范围的指标，并非与右图对比的数据错误；问题是 UX 上未足够隔离或联动。

## 已确认的中段数据与体验问题

| 优先级 | 发现 | 影响 | 修复方向 |
| --- | --- | --- | --- |
| P0 | 空投卡将 `≈ 8.96e14` 的原始池内单位直接作为核心 KPI；虽然脚注说明不与已铸数量相加，但科学计数法在主页面对普通用户不可解释。 | 与此前“原始大整数不可当经济指标”的治理原则不一致；容易被误读为规模或价值。 | 把该值降级为“原始来源单位（不可比较）”的次级技术字段，主 KPI 仅保留总池、活跃池、领取记录与快照状态。 |
| P0 | `$BEM` 矿池卡将 `7,199.9997`、`15,362.1538`、`111.8558` 等原始精度直接暴露；权重 `852,522 / 27,660` 虽有末尾 weight 标识但语义弱。 | 伪精确、扫读困难；用户难理解单位与验证比的关系。 | 对面向用户的 BEM / 权重展示进行合理舍入（如 7,200 BEM、15,362.15 BEM、111.86 BEM），原始精度留在 API；权重卡显示单位和已验证占比。 |
| P1 | 横截面组合器在“官网标签 × 处理器数”下未标注 711 未标注导致其他 4/5/2 几乎不可见，且用户不知道可切换为占比 / 对数视角。 | 研究面板可视化信息量低，少数官方标签的比较被淹没。 | 增加“数量 / 占比”切换；对标签分布默认用横向占比条并在未标注占比高时显式提示，不用无限伸长的线条。 |
| P1 | `$BEM` 的矿池、行情、最近流片、题库、公式连续堆叠，页面长度明显增加；当前只靠锚点导航跳转。 | 长页面滚动成本高，日常用户难区分“看矿池”与“查题库”。 | 将 BEM 划分为概览 / 市场 / 流片 / 题库与规则的局部标签或折叠区，默认保持概览与价格可见，其余按需展开。 |
| P1 | BEM 价格卡虽然写了第三方、流动性浅与高波动，但风险语句位于卡片底部，价格和 +79.71% 变化率更显眼。 | 容易让高波动信息变成方向性暗示。 | 将“第三方聚合 / 薄流动性 / 非投资建议”移至价格数字旁的显著状态 badge，并给 24h 涨跌加入中性标签。 |
| P2 | 最近流片卡在官方 CPU 频繁出现时会重复同一标签，信息密度不足；公式、题库与流片各自有重复的官网跳转链接。 | 长页面信息重复，重要的题库筛选区域被下推。 | 默认只显示 3 条流片并提供“全部记录”；统一 BEM 顶部来源链接，减少分散重复 CTA。 |

## 中段数据正确性

- 空投 / BEM / 价格源均明确标明来源与快照时间；本轮未发现把第三方价格写成官方价格的问题。
- BEM 公开任务数、公式和事件均有官网来源边界；问题主要是前台呈现精度、信息层级与持续滚动体验。

## 题库与规则区审计

| 优先级 | 发现 | 影响 | 修复方向 |
| --- | --- | --- | --- |
| P1 | 题库、公式规则和最近流片依次完整铺开；题库首屏信息含 6 个元卡片、长提示、搜索、两筛选器与 10 行表格，算法卡并列但高度不同。 | 桌面端可用但视觉重心不均；移动端预计会形成很长的连续滚动。 | BEM 二级导航 / 折叠分组；保留题库搜索与 10 条服务端分页，但将算法移入“规则”面板按需展开。 |
| P2 | 题库栏中的 `C_REF`、最大 Gas、NAND / LATCH 等技术字段有价值，但缺少一行“如何读这张表”的紧凑说明；当前长提示将这些字段与限制、风险混在同一段。 | 新用户无法快速理解 Gate / Depth / C_REF 与“是否上链”的关系。 | 把长说明拆为字段图例与独立限制提示；移动端采用可展开“字段说明”。 |
| P2 | 公式卡公式与定义本身清晰，但 `b_star` 与公式中的 `b*` 命名不一致，且定义表对移动扫读偏密。 | 轻微术语不一致、增加理解摩擦。 | 统一为 `b*`；将变量解释改为紧凑 definition list，限制说明维持独立警示卡。 |
| 已通过 | 题库搜索、组合/时序、上链筛选及第 1 / 31 页分页在生产 DOM 中可见，遵守每页 10 条的交互约束。 | 服务端分页没有全量拉长页面。 | 保留。 |
| 已通过 | 公式页明确不预测挖矿收益，未验证的 λ、β、Q 未当作确定事实。 | 数据治理正确。 | 保留。 |

## 协议概览、事件与目录审计

| 优先级 | 发现 | 影响 | 修复方向 |
| --- | --- | --- | --- |
| P0 | 中文 UI 的事件正文仍显示英文工程句：`Observed Circuit count increase between public registry snapshots. circuit_delta: …`。 | 主语言切换不完整，核心信息流难以快速阅读。 | 在前端按事件类别将已知 Registry 观察字段渲染为中文结构化摘要，例如“Registry 快照间新增 3 条已观察 Circuit”；未知字段保留安全原文。 |
| P1 | D1 历史折线把 Processor 与 Circuit 同置但没有明确左右轴、当前值标签或足够显著的颜色差异。 | 用户无法从折线图读出两个序列分别代表什么，也难判断增量。 | 使用明显不同色、独立轴 / 末值标签和 tooltip；若历史不足，优先显示“新增 / 累计”可切换而非两条同色累计线。 |
| P1 | 供给利用率和流片活跃度分布仅显示数量，缺少占公开 Processor 的比例；类别中的 0% / 0 电路占主导但无解释。 | 研究结论需用户心算，且易误读为协议失败而非早期 Registry 的正常分布。 | 每行显示“数量 + 占比”，添加短说明“未观察到 Circuit 不等同项目无效”。 |
| P1 | 事件流过滤器、用户策略规则、事件卡中链接与英文原文同时出现，扫读密度高；同类 1–3 Circuit 的事件连续出现时噪声高。 | 事件流看起来很“动”但信息增量小，降低关键变更可见性。 | 对短时间内同一 Processor 的同类 Registry delta 进行前端聚合预览（不改原始 API）；事件卡使用中文摘要、次级显示原文 / 证据。 |
| P2 | Creator 集中度卡已经显示公开字段边界，但没有直接显示 Top 1 / Top 3 的 Circuit 占比；目前需要读者自行计算。 | 用户难把榜单看成结构性集中度分析。 | 增加当前公开 Registry 的 Top 1 / Top 3 Circuit 占比与“地址非身份”说明。 |
| P2 | 处理器目录中极端声明供给以完整大整数直接展示，尽管完整性卡已提示原始值不应作经济指标。 | 长数字撑宽表格、造成视觉噪音。 | 表格展示压缩格式与“原始来源单位”状态，hover / 详情中保留完整值；继续保留百分比和完整导出。 |

## 已通过

- 处理器目录仍为 10 条局部分页，公开 API 与 CSV 导出入口清晰。
- 官方 / 已认证 / 社区标签的数量与“仅公开官网标签”的边界已明确，抽查的示例处理器显示为已认证。
- 策略面板明确不执行交易、不使用钱包，符合现有数据治理。

## 本地修复验收（第一轮）

- 已验证本地 Worker 使用独立 D1 schema 后，Registry、空投与 $BEM 各来源均可完成首次同步并返回 healthy；先前健康接口 500 的原因是新建本地验收库未应用项目 schema，**不是**生产或本轮前端代码回归。
- 本地首次快照没有足够 D1 差分历史时，协议时间图正确显示“无已验证覆盖”，但修复前左侧摘要会显示四个 `0`。现已改为 `—` 与虚线不可用状态，避免将“尚无覆盖”伪装成“零活动”。
- 桌面定点截图确认所选周期摘要与时间控件在同一屏内，筛选器视觉层级更清晰。无头 Chromium 的移动 `#bem` 锚点截图为空白，属于页面加载后锚点定位 / 截图时序限制，不能作为产品空白的证据；后续将使用加载后控制滚动的独立浏览器验收。

## 本地修复验收（第二轮）

真实 390 CSS 像素视口的脚本化交互验收已通过。协议脉冲在仅有首次快照、尚未形成 D1 差分覆盖时显示破折号和“无已验证覆盖”，不再展示四个看似正常的零值；切换至过去 24 小时后，控件状态会更新而摘要继续维持这一明确边界。

此前将页面扩展至 444 像素的根因是协议数据区的独立网格未在窄屏统一收缩。现已对协议、$BEM、研究和可视化网格应用 `minmax(0, 1fr)` 的单列约束，并将热力图的宽内容限制为卡片内部滚动。复测时页面文档宽度不再超过视口；截图中仅有必要的垂直滚动。

$BEM 移动端概览现优先呈现官方矿池卡和第三方行情风险 badge。日产出、累计已铸和永久放弃均按适当精度显示；题库、最近流片和规则维持在可展开的官网设计资源区，避免日常查看时拉长整页。

## 生产复核（第三轮）

生产版本 `98ef0cf` 已加载。中文模式下“所选周期协议活动”的 Registry 范围说明已完整本地化，所选周期摘要与右侧“过去 7 天 / 按日 / 北京时间”图表保持一致；生产 API 返回 6 个真实日桶、4.88 天 D1 覆盖，不补造监控开始前的零值。

空投源在内容未变化的成功合约读取后，`checked_at` 已更新且 `observed_at` 保持为最后数据变化时点；生产接口与 `data-health` 均返回 `healthy`。页面继续将极大池内数值作为“原始来源单位”次级字段呈现，而非市场规模或经济 KPI。

## 热力图空白修复审计

根因已定位为 CSS Grid 跨列错误，而不是数据缺失：热力图外层 `.heatmap-row` 有“指标标签列 + N 个时间桶列”，但内部数据容器未设置 `grid-column: 2 / -1`，因此只作为第一个网格子项放在首个时间桶列，后续列表现为空白。日期标题则独立占用全宽，造成明显错位。

修复后，日期标题加入明确的标签列占位，数据容器跨越从第二列到最后一列，并根据 API 返回的真实桶数动态生成内部网格。以当前本地 7 天日聚合的 6 个真实观察桶复核：4 行指标均完整对应 08-19 至 08-24，不存在尾部伪空白；真实零值仍呈浅色格，覆盖缺失则不渲染桶。

脚本化加载后验收进一步确认：桌面与 390px 移动端均识别到 6 个日期标题、4 个指标行，且每个指标行均含 6 个单元格。桌面截图显示日期 08-19 至 08-24 与四行指标完整横向对齐，后段不再留有虚假空白。

生产版本 `3b9a3f2` 已核验：热力图显示 08-19 至 08-24 六个实际 D1 日桶，发生铸造的处理器、电路、创建者、新增处理器四行均为六个单元格并与日期列对齐；此前后段空白已消除。生产空投卡同时显示最近检查与最后数据变化，$BEM 价格风险标签和矿池数值层级正常。

## 协议活动双面板对齐修复

生产版已验证：左侧“所选周期协议活动”和右侧“按日协议活动”的标题头部从原先 53px / 114px 的不一致，修复为同一 53px 高度；两侧说明文字均从同一垂直基线开始，外框底部也共享同一网格边界。右侧筛选器被移至说明之后，保留完整四项筛选能力而不再挤压标题行。


## 教学中心视觉验收（本地）

桌面端教学入口、三张概念卡、六步路径与安全检查的层级清晰，资源目录由 API 驱动并按来源等级展示。移动端发现 `.learn-grid` 的单列媒体查询被现有相邻布局规则覆盖：左侧路径面板没有完整占据行宽，安全面板提前并列，导致“NO RETURN PROMISE”纵向换行并挤压标题。该问题属于教学中心移动端高优先级布局缺陷，必须通过明确的窄屏单列规则修复后再进入发布验收。


教学中心第二轮视觉验收通过：独立 `learning.css` 响应式资产在真实 390px 设备仿真下使学习路径、安全检查和资源区按单列顺序展示，文档宽度为 390px，不存在横向溢出；桌面端仍保持“六步路径 + 安全检查”双栏结构。资源筛选在桌面与移动端均成功过滤为 Official 卡片。问题根因是主样式尾部的最终 `@media(max-width:680px)` 未被浏览器解析进 CSSOM；独立样式资产已规避该解析截断风险。
