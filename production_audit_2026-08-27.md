# 生产验收记录（2026-08-27）

生产地址：[TapeOut Intelligence](https://tapeout-public-monitor.tapeout-labs.workers.dev/)。本轮覆盖四项工作的部署后验收：空投 RPC 抗限流修复（`3ec1521`）、Worker 架构模块化拆分（`b201429`）、横截面组合器维度扩充（`c071b11`）、favicon 补全（本次提交）。方法：先执行完整发布契约脚本，再用真实 Chromium 对新功能做人工复核。

| 验收域 | 方法 | 生产结果 |
| --- | --- | --- |
| 完整发布契约 | `./release_contract_review.sh` 对生产地址执行 | **PASS**：教学、API 多语言、BEM 分钟刷新、官方三项目快照、NAND/LATCH K 线、数据健康与版式契约全部通过。 |
| 空投源恢复 | `/api/v1/data-health` 复查 | 修复前：drpc/publicnode/1rpc 三个网关同时 HTTP 429，数据滞后 62 分钟。修复后：状态 `healthy`，最近一次成功读取走 `bsc-dataseed.bnbchain.org`（新增的 Binance 官方节点），数据新鲜度 14 分钟。起点按小时轮转以分摊匿名配额。 |
| 架构拆分 | 部署后 10 个关键端点冒烟 | `/api/summary`、`/api/v1/catalog`、`/api/v1/analytics`、`/api/v1/bem/overview`、`/api/v1/data-health`、`/api/v1/processors`、`/api/v1/events`、`/api/v1/official-assets/overview`、`/api/v1/community/processor-leaderboard`、`/api/v1/learn/resources` 全部 HTTP 200。单文件 `worker.js`（~1970 行）拆为 15 个域模块，行为不变；5 个直读 `worker.js` 的契约脚本已重定向到新模块且断言字符串原样保留；静态断言基线（7 过 / 11 需运行环境）拆分前后一致。 |
| 组合器新维度 | 生产 API + 真实 Chromium 切换维度 | `by_supply_scale` 与 `by_circuit_density` 均返回真实分布并即时重绘：820 个公开处理器中 510 个（62.1%）声明供给在 10K–100K 档；617 个（75.2%）当前零电路。分档键为语言中性字面量，中英文维度标签经 data-i18n 生效。 |
| 事件瀑布覆盖核查 | 代码审计 + 生产事件流抽样 | `processor.created/mint_delta/circuit_delta/completed` 在产；`attestation.website_label` 历史事件可查（新事件因 2026-08-26 标签中性化治理决定停产，属刻意行为）；`market.circuit_sold_large` 已实现、待 `BSC_LOGS_RPC_URL` 专属节点 secret 配置后启用。 |
| favicon | 页面 console 检查 | 修复了每次加载必现的 `favicon.ico` 404（唯一 console error）；新增内联 SVG 图标，中性几何芯片图形，不使用任何 TapeOut 官方品牌元素。 |

> 数据边界不变：仅公开来源；私有处理器服务端过滤；不推断治理等级；新维度不引入任何新数据源或信任边界。

## 第二轮（同日）：i18n 布局修复、图表配色与术语百科

| 验收域 | 方法 | 生产结果 |
| --- | --- | --- |
| 长语言导航重叠 | 真实 Chromium 切英文后测量 | 修复前："API" 叠进 BNB CHAIN 徽章、品牌名被首个链接压住（中文短标签掩盖了该缺陷）。修复后：六个链接单行无重叠、无内部溢出，徽章组整齐换行至第二行；跨行坐标复核无真实重叠。 |
| 历史轨迹双线同色 | 生产计算样式读取 | 修复前：扁平藏青主题层把处理器/电路两条线与图例全部设为同一 `#123f88`。修复后：藏青 `#1b4d9e` + 深金 `#a87c1f`，配色经校验器验证（CVD ΔE 28.1 / 正常视觉 31.5 / 面板对比度 ≥3:1 全过）。 |
| 完成度甜甜圈 | 生产 DOM 检查 | 修复前：5 色循环配 6 个分档，末档与首档同色。修复后：6 档各自固定单色（单色调明度单调坡道，颜色跟随分档实体而非过滤后排位），加白色切片分隔线，图例带计数。 |
| 术语百科 | 生产 API + 真实 Chromium 交互 | `/api/v1/glossary` 上线（18 条双语术语，`q=` 过滤生效，已注册进 catalog/openapi/source-status）；页面卡片网格渲染 18 张，中文搜索"深度"过滤到 3 张，语言切换即时翻转术语主次语序。每条定义附官网来源链接；契约脚本约束仅官网证据域名与无收益话术。 |

> 资产缓存版本已按惯例 bump 至 `2026-08-27-glossary-r1`。

## 第三轮（同日）：全站视觉统一 r14

| 验收域 | 方法 | 生产结果 |
| --- | --- | --- |
| 设计语言盘点 | 真实 Chromium 对 17 类卡片/面板读取计算样式 | 基准族（米白平角+发丝线）占主导；发现三族异类：K 线区整块暗夜蓝孤岛（深渐变底、发光蓝边、14–18px 圆角、白字）；机制卡/生态更新卡/官方资产页签带渐变与 12–16px 圆角；两个面板级渐变背景。 |
| 统一层 | learning.css 末尾追加 r14 覆盖层 | 三族全部拉回基准：K 线区转为浅色平角面板，蜡烛在浅底重配色（#1a7f5a / #b4552d，对比度 ≥3:1 验证通过），并按金融惯例改为阳线空心/阴线实心作为 CVD 边界带要求的辅助编码；页签激活态统一为藏青实底白字；渐变与圆角全部归零。 |
| 版本钉联动 | 契约脚本全量重跑 | 首次 bump 漏了契约钉值联动导致断言 8→5，已按仓库惯例把 HTML/app.js/i18n 包/4 个契约脚本的缓存键全部推进到同一 r14 键，断言恢复 8/11 基线。 |
| 部署后目检 | 强制刷新后计算样式 + 截图 | K 线区、机制卡、资产页签、更新卡全部呈现统一语言；注意浏览器持有旧 HTML 缓存时需一次强刷（与既往 r10/r12 发布行为一致，非本轮引入）。 |

## 第四轮（同日）：多语言泄漏排查（用户报告：阿拉伯语等切换后仍有中文）

用真实 Chromium 对全部 9 种非中/英语言做过一次 CJK 文本扫描，定位到两处真实缺陷（均已修复并部署到 r16），以及一处确认为设计如此、非 bug：

| 位置 | 问题 | 修复 |
| --- | --- | --- |
| 术语百科卡片副标题 | 只要界面不是中文，副标题恒定显示中文术语（r14 引入的新功能遗留）| 副标题仅在 zh/en 两个真正有译文的语言下渲染；其余语言只显示英文主标题，不再回退中文 |
| PoD 算法说明区（`terms`/`boundaries`）| **更早存在的缺口**：该接口从未接受语言参数，服务端写死中文，导致包括英文在内的所有语言都看到中文说明 | 接口加 `locale` 参数，比照 `learningLocalization`/`ecosystemLocalization` 已有的 zh/en 双语惯例补上英文版；前端请求带上界面语言，且切换语言时重新拉取（此前只在首次加载时取一次） |
| BEM 题库表格的题目名称/难度标签（如"与门 AND"/"入门"）| 扫描命中，但核实后是**官网原始题库数据**逐字透传，不是本站文案 | 不作改动——与站内"官方原始字段保留原文，不重新翻译/推断"的既定策略一致 |
| 处理器项目名（"中本聪核心"/"小米888"等）| 扫描命中，但核实后是**链上创建者自定义的项目名称本身** | 不作改动——公开注册表数据如实展示 |

修复后在阿拉伯语界面下逐一验证：术语表卡片、算法术语定义、边界声明三处均正确显示英文，零中文残留。
