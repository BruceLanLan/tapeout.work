# TapeOut Intelligence UI/UX 修复方案（2026-08-24）

本轮以生产页面审计为依据，优先修复**时间口径不一致、原始值伪精确、信息流主语言不统一、长页面层级不足**。所有改动保持现有服务端隐私过滤、来源状态、最后成功快照与公开证据边界不变。

| 优先级 | 修复 | 实施策略 | 不做什么 |
| --- | --- | --- | --- |
| P0 | 协议脉冲与时间序列口径联动 | 左侧改为“所选周期 Registry 汇总”，随范围 / 粒度 / 时区请求刷新；跨桶活动类指标显式标注为计数合计，不声称去重。 | 不用缺失的链上交易数据冒充协议全量交易。 |
| P0 | 空投原始单位降级 | 总池、活跃池、领取记录作为主 KPI；极大剩余值显示为“原始来源单位”，加技术状态样式。 | 不将原始池内单位换算为价值或与已铸单位相加。 |
| P0 | 事件流中文结构化摘要 | 对已知 Registry 事件按类别呈现中文摘要与数值单位；保留公开证据链接和未知详情安全回退。 | 不猜测钱包身份、市场原因或未公开官方认证。 |
| P1 | $BEM 信息层级 | 用户端 BEM / 权重数值合理舍入；第三方价格在数字旁加入风险 badge；最近流片收敛至 3 条；题库与公式收纳到显式可展开的“题库与规则”。 | 不删除题库、公式或来源说明；不把行情写成官方价格。 |
| P1 | 研究图表可解释性 | 分组条图显示数值加占比并按总量缩放；Creator 面板增加 Top 1 / Top 3 Circuit 占比；历史图显示各序列最新值与明确双序列颜色。 | 不用对数或压缩视觉掩盖未标注标签占绝大多数的事实。 |
| P1 | 首屏与控制密度 | 收紧 Hero 及导航，增加首屏 KPI 密度；时间控件改为更紧凑、指标选择次级化的工具栏。 | 不移除日/小时、时区和范围选择。 |
| P2 | 长原始数量显示 | 目录表格以压缩格式展示大原始来源单位，并使用原始单位标签 / title 保留完整值和导出能力。 | 不修改 API 原始值或暗中裁剪数据。 |

## 验收条件

1. 默认“过去 7 天 / 按日 / 北京时间”下，脉冲卡、柱图和热力图使用同一选择；脉冲卡明确活动合计的非去重性质。
2. 在中文页面，常见 Registry 事件正文不再显示英文工程句；英文页面有对应简洁英文摘要。
3. BEM 面向用户指标不超过恰当小数位，且行情卡在主数值附近显示第三方和高波动风险。
4. 题库搜索、10 条服务端分页、公式与来源链接仍可用；折叠状态不影响 API 读取。
5. Mobile 390px 与桌面视觉检查无外层横向溢出；本地和生产 API / 交互契约均通过。
