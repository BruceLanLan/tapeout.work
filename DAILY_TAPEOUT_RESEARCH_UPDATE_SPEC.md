# TapeOut 每日数据与研究更新规范

## 目的

每天北京时间 09:00 对 TapeOut 官网公开数据与 TapeOut Intelligence 生产站进行一次完整核验，并生成当日带来源、可复核的中文数据分析文章。协议级累计必须以 TapeOut Day 1（上周六晚／周日早的公开启动窗口，北京时间）的零状态解释；D1 连续历史从 2026-08-19 开始，只代表本监控的后续观察边界，不替代协议起点。该任务服务于公开研究基础设施，不替代 Worker 内已配置的五分钟数据检查。

## 数据检查顺序

| 顺序 | 数据域 | 主来源 | 验证与降级原则 |
| --- | --- | --- | --- |
| 1 | Registry | `https://tapeout.net/processors.json` | 与 `/api/v1/summary`、`/api/v1/analytics` 对照 Processor/Circuit；隐私过滤只允许在服务端聚合后公开。 |
| 2 | 协议健康 | `/api/v1/data-health` | 记录 registry、airdrop、bem mining、taskbank、miner index、price 的 `healthy/stale/error/pending` 与最后成功时间。 |
| 3 | $BEM 挖矿 | `https://tapeout.net/pod/pod-mainnet.json`、`pod-stats.json`、`pod-miners.json` | 只使用官网公开快照和已验证的官网根 RPC 只读回退；失败不显示零值。 |
| 4 | $BEM 题库与规则 | `pod-taskbank.json`、`pod-refimpl.json` 与官网 Algorithm 文案 | 仅报道官网披露的题库、算法和公式；不推导未公开参数或收益。 |
| 5 | $BEM 市场 | `/api/v1/bem/price` | 明确为第三方聚合；记录实际 provider、池地址、流动性和状态，绝不称为官方价格。 |
| 6 | 空投 | `/api/v1/airdrop-overview` | 单独记录快照状态；`stale` 只可作带时间戳的历史观察。 |

## 网站更新规则

网站的 Registry、空投与 $BEM 子域由生产 Worker 每五分钟独立刷新。每日任务只在下列情形修改网站代码：官网公开源新增稳定字段、源结构变化、连续健康异常、字段显示失真、或契约/视觉回归。任何修改必须保留：来源隔离、6–8 秒级超时、最后成功快照、哈希去重、明确的健康状态、服务端隐私过滤与本地/生产回归。代码改动须通过测试后推送 `main`，由既有 GitHub → Cloudflare 自动构建发布。

## 文章更新规则

每日文章必须使用固定的“统计截止时间”，并把三类事实分开：官网/链上只读、TapeOut Intelligence 聚合、第三方市场快照。协议总量、首周/累计变化应从 TapeOut Day 1 的零状态叙述；若使用 2026-08-19 之后的 D1 曲线，只能标为“监控可比观察窗口”，不得误写为协议启动日。正文必须使用引用、图表或表格，且至少报告 Registry 规模、Circuit 结构、$BEM 矿池/题库状态、近 24 小时活动、数据健康与关键变化。不能把极端原始铸造单位相加为经济规模，不能推断钱包控制人，不能把短期价格变动写成价值判断或投资建议。

若当天没有数据变化，文章应明确写“截至观察窗口无已验证的结构性变化”，而不是为了日更虚构观点。若关键源异常，文章应保留最后成功值、写明 `stale/error` 和观测时间，并报告排障状态。

## 每日交付物

每日任务在报告目录保存以下文件：当日中文报告、原始公开快照或其哈希、指标汇总 JSON、数据图表，以及简短运行日志。向用户交付时须说明网站健康、是否发生网站代码部署、当日核心变化与已知数据限制。
