# TapeOut 发布候选版研究记录

**记录日期：** 2026-08-19

## 1. 官网全协议口径

TapeOut 官网将协议定义为链上硬件制造基础设施：公开的 Processor Registry、Processor 的 ERC-721 身份、电晶体 ERC-1155、将电晶体消耗为 Circuit NFT 的 Tape Out 行为，以及可组合的既有电路。官网同时公开 Factory proxy、Genesis Processor 和 Genesis 电晶体 ERC-1155 合约地址。

因此 Dashboard 的“协议活动”必须明确表示为：**从 TapeOut 官网公开 Registry 观察到的全协议 Processor、Mint、Circuit、Creator 与官网展示标签数据**。它不是对所有 BNB 链交易、所有钱包行为或仅认证项目的统计。现有 `processors.json` 是主稳定源；全协议指标应从其全部公开且通过隐私过滤的记录聚合，而非只从 `official/certified/community` 子集聚合。

## 2. 官网空投模块

从 TapeOut 官网公开前端资产中核验到以下内容：

| 项目 | 可公开核验的定义 |
| --- | --- |
| 页面入口 | 官网导航使用 `#airdrop`；创建入口使用 `#deploy-airdrop`。 |
| 对象 | Factory-registered Processor 的 ERC-1155 电晶体。 |
| 创建机制 | 创建者将指定电晶体数量存入 Airdrop contract，设定每地址领取额；可取消并取回未领取余额。 |
| 领取机制 | 每个地址至多领取一次；无需 approval；合约直接发送电晶体。 |
| 风险披露 | 开放池没有身份门槛；官网明确提示高价值、分拆份额过多的池可能被机器人迅速领取。 |
| 合约性质（官网文字） | 无 owner、不可升级、不可暂停、无费用；只接受 Factory 注册 Processor 的电晶体。 |

目前公开静态资源确认了功能与 UX 文案，但尚未从公开页面中确认 Airdrop contract 地址或可批量读取的独立 API。因此发布候选版可以先纳入**官网空投功能状态与证据入口**，不得展示“总空投数量、已领取量、领取钱包”等未经链上合约地址和事件 ABI 复核的数值。

## 3. 指定 Dune 参考

### Bitcoin Tokens: BRC-20/ORC-20/SRC-20/BRC-721

该看板将多种资产类别按时间维度和指标维度交叉：日度 mint、交易数、费用、大小以及类别市场份额。它使用折线/总值、饼图和柱状图将**绝对规模**与**百分比构成**并列，而不是只显示单点总量。

可直接迁移到 TapeOut 的模式：

1. 日度的新增处理器、已铸电晶体、新增电路、活跃创建者柱图；
2. 官网标签和已铸完成度的组成饼图；
3. 热力图：日期 × 指标强度，前提是日度快照充分累计；
4. 保留 Registry 字段为核心，不把没有稳定链上源的成交额或钱包身份猜测放进正式 KPI。

### Inscription BRC-20 Weight and Percentage

该链接为动态 Dune 页面，文本抽取仅返回 Loading，不能仅据此虚构指标。发布版只吸收“权重/占比的时间序列、绝对数与归一化百分比切换”的通用交互思路；具体图表必须基于 TapeOut 可验证数据重建。

### Dune 图表能力

Dune 文档公开说明支持 Bar、Area、Scatter、Line、Pie 与 Mixed Chart；同一图允许多条序列、堆叠、百分比归一化、左右轴。TapeOut 首批适配优先级应为：日度柱图 + 指标切换、时间热力图、完成度/官网标签构成图、Mint 完成度 × Circuit 的散点图，以及在日度数据成熟后增加柱线混合图。

## 4. 视觉方向

官网的 Logo 和核心界面以深蓝为主，黄色仅作为强调。发布候选版应进一步让蓝色承担：品牌、导航、图表主序列、边框和结构；浅蓝用于辅助序列；黄色仅用于状态变化、警示或强调，避免其充当大面积第二主色。

## 5. 公开来源

1. https://tapeout.net/
2. https://dune.com/hashed_official/bitcoin-tokens
3. https://dune.com/murchandamus/inscription-brc20-weight-and-percentage
4. https://docs.dune.com/web-app/visualizations/charts-graphs

### 2.1 空投合约与读取接口已核验

进一步读取官网公开 `chain-B6rwqWks.js` 配置模块后，确认 `DEPLOYMENT.airdrop` 已设置为：`0x7Fd055496b638aD81f58B33Fd04d6e90bbC2a672`。官网前端 ABI 明确包含：

- `list()`：返回 Airdrop 记录列表；
- `claimed(uint256 ...)` / `claimedBy(address ...)`：读取领取状态；
- `claim(uint256 dropId)`：领取；
- `Claimed(...)` 事件；
- 合约记录包含 `perClaim`、`remaining`、`claimedCount` 等字段。

官网链配置将 BNB Mainnet 读取端点配置为 `https://bsc-dataseed.binance.org`，并同时在 `READ_RPC` 中列出 `https://bsc.drpc.org`、`https://bsc-rpc.publicnode.com`、`https://1rpc.io/bnb` 与 `https://rpc.ankr.com/bsc`。这些是官网前端的读取策略，不等同于长期稳定的数据 SLA。Dashboard 若用此合约做全协议空投数据，应使用低频 `eth_call list()` 读取并保留“官网公开合约配置”证据；若后续需要按领取地址或日度领取量，则应使用独立、具明确配额的 BSC 索引服务。

## 6. 视觉审查 Round 1（桌面 1440px）

本地无头浏览器截图确认蓝色已成为页面主色，品牌、KPI、边框、链接和信号均使用同一深蓝家族，砂金不再主导视觉。首轮同时发现两项需要修复的可用性问题：顶部导航在纸白背景下对比度不足；“全协议公开注册表”在协议脉冲面板的窄列中换行过多，导致信息密度失衡。后续审查应把协议脉冲改为更均衡的两栏，并提高顶部导航与细节文字的对比度。

### 视觉审查 Round 2（桌面 1440px）

修复后顶部导航的深蓝对比度更清晰，协议脉冲已改为均衡双栏；全协议定义能以“全协议公开注册表 · 当前 UTC 日”及范围说明完整呈现，不再被窄列挤压。截图亦确认主视觉蓝色已经高于砂金。下一轮应检查折叠事件流的可见内容数量：当前默认仍载入大量事件，虽然有“Show all loaded events”按钮，但在首屏下方的纵向密度仍需用功能性限制验证。

### 视觉审查 Round 3–4（移动端 390px）

信息流默认折叠已生效：只显示 10 条，且折叠状态不再触发连续分页加载。移动端截图同时显示原有事件条目的“时间列—信号点—正文列”并排网格使正文过窄，长项目名和说明被过度折行。下一项修复应把移动端条目改为垂直时间戳 + 全宽正文的两列结构，使信号线保留但正文获得完整可读宽度。

## 发布审查进度

| 轮次 | 主题 | 结果 | 处理 |
| --- | --- | --- | --- |
| 5 | 移动端信息流折叠 | 初始实现会在折叠状态下由哨兵连续加载 | 已改为默认 10 条；仅在用户展开后才允许自动续页。 |
| 6 | 移动端事件条目可读性 | 原三列时间线使正文过窄 | 已改为窄屏“信号线 + 时间戳 + 全宽正文”的垂直布局。 |
| 7 | 官网命名一致性 | 英文界面出现 `Tapeout` 与 `TapeOut` 混用 | 已统一所有可见英文文案为 `TapeOut`。 |
| 8 | API 与页面契约 | 本地端到端核验 | 通过：全协议范围、空投概览、28 日活动、健康状态、10 条目录分页、局部分页与前端容器均存在。 |
| 9 | 五分钟差分刷新 | 连续触发两次本地计划任务 | 首次写入一条新快照后，第二次返回 `no_change`、`changed_processors: 0`；证明无变化时不会重复写入 Registry 快照。 |
| 10 | 可选源降级隔离 | 市场数据源未配置、Registry 与空投均健康 | 通过：核心 Registry 最近检查为 1 分钟、空投来源健康且最近运行 `no_change`；市场状态明确为 `not_configured`，未阻塞任何核心数据 API。 |
| 发布验收 | Cloudflare 生产截图与实时 API | 通过：生产版本已显示蓝色主视觉、默认 10 条事件流、全协议范围说明、日度柱图、空投摘要、热力图、研究面板局部分页与目录分页；Registry 与 Airdrop 数据源均已在生产五分钟任务后健康运行。 |

## 发布前精修审查（字段、单位与图表口径）

| 审查项 | 发现 | 修复与结果 |
| --- | --- | --- |
| 首页 KPI | 原“来源报告的已铸晶体管”会被个别极大原始值放大，首页读者容易误读为经济量或协议总量。 | 改为“发生过铸造的处理器”计数；原始单位只保留在明确命名的次级图表选项中。 |
| 协议脉冲 | 原始 Mint 增量不适合和处理器、电路、创建者在同一 KPI 行直接比较。 | 改为“发生变动的处理器”计数；保留 API 原始 Mint delta 供专业用户使用。 |
| 空投 | `remaining` 的合约字段不能表述为“剩余电晶体”。 | 统一改为“剩余池内单位 / remaining pool units”，并明确不与处理器目录 Mint 相加。 |
| 日度图表 | 28 日标签在热力图中拥挤；默认原始 Mint 单位不利于可比分析。 | 默认窗口缩短为 14 个 UTC 日；默认指标为“发生铸造的处理器”；热力图也只使用计数型指标。 |
| 时间 | 页面没有明确时间区，可能造成跨时区误读。 | 所有完整时间戳强制以 UTC 显示；协议活动范围保持 UTC 日口径。 |
| 导入基线 | 首次全量 Registry 导入可能被误读为当天新增活动。 | 以首次完整快照为基线过滤事件流、协议脉冲和日度指标；同时修正名称字段未读出导致的全表误写。 |
| 桌面与移动端 | 精修后复核。 | 通过：桌面与 390px 窄屏均显示可解释的计数 KPI、十条默认事件流和更紧凑的日度图表。 |
| 热力图最终复核 | 28 日标签拥挤 | 已固定为最近 14 个 UTC 日，并将 CSS 网格同步为 14 列；桌面与移动端单元格和日期均可读。 |
| 协议脉冲最终复核 | 原始单位与计数并列会造成量纲混淆 | 主面板统一使用新增处理器、发生变动处理器、新增电路和已观察创建者四个计数指标。 |
| 精修视觉验收 | 1440px 桌面截图 | 通过：首页 KPI、默认日度指标、空投字段、热力图、十条信息流与局部分页均保持可解释的量纲和蓝色层级。 |
| 市场数据降级 | 生产截图发现未配置专属 BSC 数据源时仍可显示遗留成交统计。 | 修复：只有 `data-health.market.status = healthy` 且存在扫描覆盖区块时才展示成交卡；其他状态均展示明确的未配置或延迟说明。 |
| 日度历史缺失 Bug | 前端固定生成最近 14 日的零值序列，导致观测开始前的日期被误画为零活动。 | Worker 现返回 `coverage_start`，仅从首个可信 Registry 差分开始生成序列；图表在仅有单日历史时显示“历史仍在积累”，并明确此前日期不可用而非零活动。当前可审计日度差分从 2026-08-19 UTC 开始。 |
| 信息层级重排 | 用户要求将每日协议数据、已铸进度与“已打满”相关指标前置。 | 已将“协议脉冲（含每日协议数据、空投与热力图）”设为第 01 节，将“网络概览（含供给利用率/100%+ 已全量铸造）”设为第 02 节，事件流后移至第 03 节。桌面截图验证首屏后的阅读顺序符合此优先级。 |
| 前置模块移动端验收 | 前置后的每日协议模块在仅有单日历史时曾产生较大空白。 | 已增加 `is-empty` 紧凑状态；移动端截图确认协议脉冲、每日协议说明、空投和热力图依次可见，历史积累提示不再占用大面积无效高度。 |
| 日度趋势与热力图空状态修复 | 用户截图显示单日历史时日度模块大面积空白，热力图仅有单列色块且标签拥挤，灰蓝文案对比度不足。 | 日度图表保留紧凑积累提示并取消相邻卡片高度拉伸；热力图在少于两个 UTC 日时改为紧凑虚线积累说明，隐藏单列色块；说明、选择器、覆盖期和空状态均提升为墨蓝高对比度。桌面与移动端渲染通过。 |
| 近 24 小时滚动分析修复 | 用户要求未满自然日时按过去 24 小时展示，不可留空。 | 日度接口改为从真实 `public_events` 取最近 24 小时、按 1 小时聚合的五分钟差分观察；本地 API 返回 `mode=rolling_24h`、`bucket_minutes=60` 和多个真实小时桶。桌面与移动端渲染确认柱状图和热力图已填充真实活动，不再显示等待第二个 UTC 日的空状态。 |
