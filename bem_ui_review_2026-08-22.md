# $BEM UI 本地视觉审查（2026-08-22）

| 视图 | 结论 | 发现 |
| --- | --- | --- |
| 桌面首屏，1440×2200 | 通过初步层级审查 | 新增 `$BEM · Proof of Design` 紧接协议概览，蓝色标题与卡片边界清晰；首屏底部已出现全网日产出、在挖矿机、已验证权重和第三方价格卡。需单独审查下方题库与公式区。 |
| `$BEM` 锚点桌面，1440×1550 | 截图方法异常 | 无头 Chromium 通过 `#bem` 初始锚点截图只产生空白背景（9.8 KB），不能作为视觉验收证据。该问题需要改用加载后脚本滚动或更高视窗的截图方式复核，不能误判为页面空白。 |

已确认本地接口实际返回数据：矿池来源为官网公开快照；价格源为 DexScreener 第三方聚合；题库默认分页为 306 总题、每页 10 条。后续验收需要同时覆盖桌面和移动端的题库表格、分页及算法公式区。

## 加载后局部截图复核

| 视图 | 结论 | 发现与动作 |
| --- | --- | --- |
| 桌面，1440×1675 | 基本通过 | 矿池、第三方价格、306 道题的局部 10 条表格、分页与公式边界在同一视觉段中清晰可读。价格风险警告突出；矿池若超出新鲜度阈值明确显示为最后成功快照。将把矿池指标由单一“已验证权重占比”补齐为**已验证 / 未验证权重**实际数值。 |
| 移动，390×3143 | 需要复测 | 内容总体可读，风险条、卡片与公式区没有重叠；题库桌面表横向滚动为预期行为。但当前调试脚本使用 `mobile: true` 的设备仿真，可能产生超出 390 CSS 视口的布局缩放，导致第二个筛选控件和部分题库元数据的可见性无法作为最终依据。下一轮改用 390 CSS 像素宽度、非移动缩放的模拟复核。 |

视觉审查确认：任何 `stale` 状态会保留最近成功读数并在矿池卡下以显著警示显示，未展示零值或伪实时数据。

## 最终移动端修复验收

固定 390 CSS 像素宽度截图复核通过。`$BEM` 区保持两列关键指标，**已验证 / 未验证权重**以可读的换行数值完整展示。题库卡片保持 390 像素容器宽度：元数据为 2×2、搜索框独占一行、类型和上链资格选择器各占半行；宽表格在卡片内部横向滚动，不再挤压或裁切筛选控件。算法公式和“解读边界”在窄屏中同样完整可读。

视觉验收结论：桌面与移动端均有真实截图复核；页面清楚区分官网矿池 / 官网题库 / 官网公开规则 / 第三方价格，并将价格风险与矿池陈旧状态显示在相应数据域内。

## 发布前数据与降级验收

| 验收项 | 结果 | 证据 |
| --- | --- | --- |
| Worker / 前端语法与静态结构 | 通过 | `node --check src/worker.js`、`node --check public/app.js`、`git diff --check` 通过；扩展发布回归通过。 |
| $BEM API 契约 | 通过 | 矿池概览含真实矿池数值、合约和来源；价格明确为第三方且返回池地址、流动性、24h 指标和风险文案；题库第 1 / 2 页均精确 10 条，公式和边界齐全。 |
| 题库筛选 | 通过 | `kind=seq&onchain=true` 返回 76 条匹配中的首 10 条，10 条均为时序且可上链。 |
| 价格提供商故障 | 通过 | 向隔离本地 D1 写入一次受控失败运行后，`/api/v1/bem/price` 与 `data-health.bem.price` 均为 `stale`，保留同一美元价格、流动性、交易对和第三方风险提示；不存在 `$0` 回退。 |
| 静态题库去重 | 通过 | 多次计划任务后 `bem_catalog_snapshots = 1` 且最后目录运行状态为 `no_change`。矿工聚合索引独立更新（3 个快照），未触发题库重复写入。 |
| 数据域隔离 | 通过 | 计划任务中 Registry、空投、可选市场和 $BEM 通过独立 `waitUntil` / `Promise.allSettled` 执行；单一价格失败不覆盖或阻断矿池和题库最后成功快照。 |
